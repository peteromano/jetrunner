(function(undefined) {
    'use strict';

    module.exports = {
        cli:        require('./cli'),
        server:     require('./server'),
        test:       require('./test')
    };

})(undefined);