(function(undefined) {
    'use strict';

    module.exports = {
        cli:        require('./cli'),
        server:     require('./server'),
        run:        require('./run'),
        version:    require('./version')
    };

})(undefined);